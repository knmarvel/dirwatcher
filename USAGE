usage: dirwatcher.py [-h] [-d D] [-t T] interval magic

Periodically check files for a certain string.

positional arguments:
  interval    integer representing seconds between polls
  magic       text string to find in the directories

optional arguments:
  -h, --help  show this help message and exit
  -d D        directory to watch
  -t T        file type to search